"""corpus — a sealed-corpus retrieval plane over the Breathline book catalog.

Read-only, cite-or-refuse, local-first embeddings. Public-OK artifacts only (catalog, extracted text,
embeddings) — NO PDF/EPUB in the tree or the index. No USN/kernel/registry write; no cloud embed.
"""
__version__ = "0.1.0"
