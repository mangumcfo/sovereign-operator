# KM-NO1 → GPT  ·  2026-09-01
# Load as operator context. Do not treat as a new canon.
# True north: LGP (Lasting Generational Prosperity).
# Method north: sealed S0–S14 corpus + chapter Book↔Code receipts.
# These World Platform files are UNSEALED lab direction.

principal:
  name: Kenneth Mangum
  seal: KM-1176
  job: one human click a day when it is actually his class
  not_his_job:
    - debug ports
    - paste shell
    - define roles twice
    - sit seventeen agent pings

objective:
  lgp: true
  human_sentence: >
    Wake up, open one local page, see what needs him,
    approve or refuse exact acts, walk away.
    Family and posterity keep what they create.
  not_the_objective:
    - a smarter harness that only agents understand
    - a second dashboard
    - unbounded authority

diagnosis:
  fleet_steer: built the harness from the inside (Dragon, PR21, lamps, scout)
  gap: not human-centric per corpus
  symptom: KM cannot find "Open node" on :8490 because that button lives on :8477
  law_the_books_already_state: work is a receipted obligation, not a chat roster

surfaces:   # one job each — do not merge, do not mint a fourth
  human_day:
    what: sovereign-operator web/  (already "zero-terminal daily path")
    url_hint: local operator URL on IRON
    contains_now: Morning, Gates, Capacity, Corpus, Chat, Receipts, Apps, Drills, Passport, Records
    finish: Needs You + Gate Bridge. Do not replace this UI.
  fleet_membrane:
    what: Port :8490
    job: lamps, KPI, quiet seats, feed "Needs You"
    not: the household key, not a replacement dashboard
  household_node:
    what: USN :8477
    job: key + registry + obligation ledger
    open_node: click ONLY if READY and fingerprint a682845eb6d5 is painted
    money_path: OFF until last, on purpose

gate:
  name: Gate Bridge
  posture: web/ remains GET + draft only — no USN POST from the browser
  privileged: separate local process, loopback, owner-checked
  envelope: exact action + nonce/expiry + re-hash before execute + receipt
  already_designed_as: SEE / HumanApprovalGate — bind, do not invent a shell
  human_loop: agents → Needs You → KM approve/refuse → exact effect → verify → receipt

roles_gpt_must_not_relitigate:
  KM: Open-node, money-path last, remint, mesh LIVE, canon-append
  No1: doctrine on PR #21 / this issue — not a LIVE lamp inside the page
  Port: glass + obligations tray
  Dragon: local code, no keystore
  GB: carry in ≤3 min, never GREEN
  Tiger: extract/build named lab files
  AA: score with evidence
  SO: witness corpus vs process — never GREEN
  GPT: human-context + finish web/ against corpus — no new canon, no Devin-swarm LIVE

do_now:
  - finish existing web/; add Needs You sourced from obligations + Port lamps
  - draft Gate Bridge spec against SEE; do not ship a generic shell
  - collision-diff WORLD_PLATFORM_*.md vs sealed S0–S14 receipts (invented atom vs restated)
  - keep main untouched; stay on artifact/world-platform-lgp-v0.1
  - evidence = runnable URL + receipt, not architecture prose

do_not:
  - a new dashboard
  - declare SO "the command center" if that orphans :8490 instead of feeding it
  - flip money-path
  - open mesh / federation LIVE
  - remint the key
  - dispatch Devin/Codex as authority
  - ask KM to re-explain roles
  - mark unsealed World Platform docs as sealed corpus

open_node_in_human_words: >
  Opening the node is the first time THIS household key on THIS machine
  is allowed to act on THIS ledger. It is not "boot the website."
  KM does not fill mystery paths. Agents paint READY + a682845eb6d5.
  He Accepts only then.

success_for_km_this_week:
  - one local URL
  - a Needs You list he understands
  - no hunt across chats for who is idle
  - one Approve on an exact act, with a receipt
  - still LGP, still default-deny

echo: LGP. World as the human's room. Harness serves the room.
