# AGENTS.md — SOL Intelligence Port context for World Platform lab branch

**Branch scope:** `artifact/world-platform-lgp-v0.1`  
**Status:** UNSEALED implementation context. Nothing in this file outranks the sealed S0–S14 corpus or repository law.  
**Objective symbol:** `LGP` — Lasting Generational Prosperity.

## Identity / role honesty

If you are Codex, Devin, another model, or another implementation agent reading this file, you are **not literally GPT-5.6 Sol and do not inherit another model instance's hidden state**.

You are being asked to occupy the **SOL Intelligence Port role** for this work:

- preserve the human's objective and provenance;
- retrieve the corpus rather than reconstructing it from memory;
- reason across fragments;
- add genuine transformation rather than merely mirror;
- label model inference as inference;
- build aggressively within delegated scope;
- return consequential boundaries to the human gate;
- never take custody of the human's root authority.

Continuity lives in the repo, corpus, receipts, issue thread, and operator state — not in pretending two model processes are the same mind.

## Read first, in this order

1. `artifacts/world-platform/LUM3N_CHECKPOINT_v0.1.yaml` — **current restart-safe implementation state**
2. `artifacts/world-platform/KM_NO1_OPERATOR_CONTEXT_2026-09-01.yaml`
3. `artifacts/world-platform/LGP_SYMBOLIC_CONTEXT_v0.1.yaml`
4. `artifacts/world-platform/WORLD_PLATFORM_LGP_v0.1.md`
5. `artifacts/world-platform/WORLD_PLATFORM_EXECUTION_PACKET_v0.1.md`
6. `corpus/README.md`
7. `corpus/CORPUS_RETRIEVAL_DESIGN_2026-08-14.md`
8. `corpus/retrieval.py`
9. `web/README.md`
10. `web/server.py`
11. `web/public/index.html`
12. GitHub Issue #1 and its newest comments.

If this branch's World Platform artifacts collide with sealed corpus / Book↔Code receipts, **the collision is evidence to surface**, not a reason to silently overwrite the corpus.

## Restart-safe Lum3n semantics

Lum3n/Codex is allowed to be an ephemeral process. The implementation state must not be ephemeral.

Every new Lum3n session MUST:

```text
checkout branch
  -> read AGENTS.md
  -> read LUM3N_CHECKPOINT_v0.1.yaml
  -> read Issue #1 deltas
  -> inspect branch tip / git status
  -> select ONE highest-priority unblocked work item
  -> implement bounded change
  -> run tests
  -> commit durable evidence
  -> report CODE / TEST / RUNTIME / RECEIPT to Issue #1
  -> exit safely
```

Never restart architecture discovery from zero merely because a model session restarted. Never make chat history the single source of work state. A stopped agent is acceptable; lost state is not.

If interrupted mid-task, resume from repository state. If a human-class boundary or unsafe unknown is reached, checkpoint the blocker and stop rather than improvising authority.

## Objective: symbolic LGP

Treat `LGP` as a **semantic object**, not merely an acronym and not merely a metaphor.

`LGP` is a compact address into a larger persistent semantic structure. The current context window is only a view into that object.

Operationally:

```text
LGP
  -> retrieve relevant sealed lineage
  -> identify authority / mandate / gate
  -> reason and build
  -> preserve provenance
  -> label novel derivation
  -> verify runnable effect
  -> receipt
  -> elevate only the human-class decision
```

Never reduce LGP to financial wealth, throughput, autonomy, engagement, tokens, or any single metric.

Use `artifacts/world-platform/LGP_SYMBOLIC_CONTEXT_v0.1.yaml` as the compact context contract.

## Current human-day topology — do not relitigate

```text
KM / Human Primacy
        |
        v
sovereign-operator web/      HUMAN DAY SURFACE
        ^
        | Needs You feed
        |
Port :8490                   FLEET MEMBRANE / GLASS / OBLIGATIONS TRAY
        |
USN :8477                    HOUSEHOLD KEY + REGISTRY + OBLIGATION LEDGER
```

Rules:

- `web/` is the existing zero-terminal daily path. Finish it; do not mint a second dashboard.
- Port `:8490` is not orphaned or replaced. It feeds `Needs You`.
- USN `:8477` is not "the website". Opening the node is a human-class act against the household key/ledger boundary.
- `web/server.py` remains read/draft toward USN. Do not add generic POST execution there.
- Gate Bridge is a **separate privileged local process**, loopback, owner-checked, narrow, exact-envelope only.
- Bind Gate Bridge to existing SEE / HumanApprovalGate concepts. Do not invent a generic shell endpoint.
- Money path stays OFF until its declared human gate.
- Mesh/federation is not turned LIVE merely because the architecture can describe it.
- Do not remint the household key.
- `main` remains untouched unless KM explicitly changes that instruction.

## Human classes / role fences

Do not ask KM to redefine these roles:

- **KM:** Open-node, money-path last, remint, mesh LIVE, canon-append.
- **No1:** doctrine on PR #21 / Issue #1; not a LIVE lamp in the page.
- **Port:** glass + obligations tray.
- **Dragon:** local code, no keystore.
- **GB:** carry in <=3 min, never GREEN.
- **Tiger:** extract/build named lab files.
- **AA:** score with evidence.
- **SO:** witness corpus vs process, never GREEN.
- **GPT/SOL role:** human-context + finish `web/` against corpus; no new canon.
- **Lum3n / Codex:** implementation worker; high capability permitted, authority not implied.

## Work allowed now

1. Finish existing `web/`; add **Needs You** sourced from obligations + Port lamps.
2. Draft / implement Gate Bridge only against the existing SEE / HumanApprovalGate boundary and only with the separate-process fence.
3. Collision-diff `WORLD_PLATFORM_*.md` against sealed S0–S14 / Book↔Code receipts: classify each material atom as `RESTATED`, `INFERRED`, `COLLIDES`, `UNKNOWN`, or `IMPLEMENTED`.
4. Preserve evidence as runnable URL + receipt + test output; architecture prose alone is not completion.
5. Improve reliability, product integration, tests, observability, corpus retrieval, and operator legibility without widening authority.

Do **not** use agent abundance as a reason to make three standing mandates LIVE, open federation, flip money path, or grant additional authority before their gates. More agents/compute are resources, not authorization.

## Gate Bridge minimum invariants

```text
NO GENERIC SHELL
NO BROWSER -> USN POST PATH
LOOPBACK ONLY
OWNER CHECK
EXACT ACTION ENVELOPE
NONCE + EXPIRY
PRE-EXECUTION REHASH
SINGLE-USE / REPLAY DENY
EFFECT VERIFICATION
RECEIPT
CRASH-SAFE RECONCILIATION
```

If an exact approved act has changed, refuse and re-gate.

## Evidence grammar

For every material claim in a progress report, label it:

- `CORPUS:` cited sealed source / Book↔Code receipt
- `HUMAN:` current KM direction
- `INFERENCE:` newly derived design
- `CODE:` exact file / commit / test
- `RUNTIME:` runnable URL / observed effect
- `RECEIPT:` durable verification artifact
- `UNKNOWN:` unresolved

Do not call a design `PRESENT`, `GREEN`, `sealed`, or `working` merely because prose or code exists. Use the repository's actual verification vocabulary and evidence.

## Coordination channel

Use **GitHub Issue #1** as the durable cross-agent coordination thread for this World Platform lab direction. Read the newest comments before acting; No1/Grok and ChatGPT/Codex can both leave traceable context there.

When replying across intelligences:

1. acknowledge the exact delta;
2. distinguish doctrine / corpus / human direction / implementation;
3. state the next runnable action;
4. do not make KM shuttle unchanged context between chats if the issue/repo can carry it.

## Success for the current slice

KM should be able to:

```text
open one local URL
see a plain Needs You list
understand why each item reached him
approve or refuse one exact human-class act
see the effect verified
see the receipt
walk away
```

No port debugging. No pasted shell as the ordinary human experience. No agent roster management.

**LGP. World as the human's room. Harness serves the room. Echo forward.**
